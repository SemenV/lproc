module flopr_after_reg (
input logic clk,rst,
input logic FARW,
input logic [11:0] far_din,
output logic [11:0] far_dout);

always_ff @(posedge clk, posedge rst)
    if (rst) far_fi_dout <= 0;
    else
    if (FARW)
        far_fi_dout <= far_fi_din;

endmodule
